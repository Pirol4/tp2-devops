apiVersion: batch/v1
kind: Job
metadata:
  name: filipesantos-ml-job-ds2   
  namespace: filipesantos  
spec:
  template:
    spec:
      containers:
      - name: ml-generator
        image: pirolaa/ml-generator:v9
        volumeMounts:
        - name: model-storage       
          mountPath: /data           
      volumes:
      - name: model-storage         
        persistentVolumeClaim:
          claimName: filipesantos-pvc 
      restartPolicy: Never 
  backoffLimit: 2 
